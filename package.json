{
  "name": "youtube-transcript-summarizer",
  "type": "module",
  "packageManager": "bun@1.3.5",
  "scripts": {
    "start": "if [ -n \"$NO_COLOR\" ]; then unset FORCE_COLOR; fi; bun run src/youtube.ts",
    "youtube": "if [ -n \"$NO_COLOR\" ]; then unset FORCE_COLOR; fi; bun run src/youtube.ts",
    "video": "if [ -n \"$NO_COLOR\" ]; then unset FORCE_COLOR; fi; bun run src/youtube.ts",
    "doctor": "if [ -n \"$NO_COLOR\" ]; then unset FORCE_COLOR; fi; bun run src/youtube.ts --doctor",
    "typecheck": "env -u FORCE_COLOR tsc --noEmit",
    "test": "env -u FORCE_COLOR bun test src",
    "eval": "if [ -n \"$NO_COLOR\" ]; then unset FORCE_COLOR; fi; bun run src/eval.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.5.0",
    "axios": "^1.16.1",
    "dotenv": "^17.4.2",
    "marked": "^18.0.6",
    "openai": "^6.39.1"
  },
  "private": true
}
